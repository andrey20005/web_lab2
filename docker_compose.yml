services:
  wildfly:
    image: quay.io/wildfly/wildfly
    ports:
      - "8080:8080"
    volumes:
      - ./build/libs/web_lab2-1.war:/opt/jboss/wildfly/standalone/deployments/web_lab2-1.war
    networks:
      backend:
networks:
  backend: